2025-12-2
数据库数据删除失败  
原因：没有加入事务
- 增删改都需要加入事务@Transactional
- 事务保证了单线程的保持原子性，也保证了多线程的隔离性。

12-08
加入可视化界面，添加登陆/书架功能/用户定位功能

2025-12-09
- 引入统一返回体 CommonResponse<T>，新增 GlobalExceptionHandler 做全局异常处理
- AuthController、ReadingController、UserBookshelfController、AiReadingController 等接口改为返回 CommonResponse
- BookshelfService 和 ReadingService 为增删改方法加上 @Transactional，查询方法加 @Transactional(readOnly = true)
- BookshelfService 的 addToBookshelf / removeFromBookshelf 改为幂等处理
- reader.html 前端适配统一返回体：书架、阅读进度、AI 提问等接口统一解析 resp.code / resp.data / resp.message

12-10
- 修复了无法导航到书籍的问题
- 将pdf拆分为多个txt文件，但是效果并不好。

12-24
- 计划安排：  
    - 1月中旬以前：打牢基础 + 吃透当前项目的关键模块
Java 语法 + 集合 + 异常 + 面向对象，系统再过一遍（每天写一点小 demo）
针对当前项目：
深入理解登录注册 / 用户状态、书本+章节、进度记录这几块
尝试在项目里自己再新增几个接口（比如新增一个“收藏章节”、“最近阅读列表”）
    - 一月中旬到过年之前：新做一个小项目，尽量独立完成
选一个比阅读器简单的业务，自己从 0 设计 + 实现：
记账 / 待办 / 简历管理 / 简单讨论区等等
重点练习：
接口设计（URL 结构、请求/响应体）
表结构设计
Controller-Service-Repository 三层分工